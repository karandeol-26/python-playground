# This program uses Python's turtle module to generate a Hirst-style dot painting.
# The turtle starts in the bottom-left corner and draws a 10x10 grid of evenly spaced dots.
# Each dot is assigned a random RGB color, creating a colorful, generative art pattern.
# This script is part of my Python playground for experimenting with turtle graphics and visuals.

import turtle as t
import random

tim = t.Turtle()
screen = t.Screen()
tim.speed("fastest")
t.colormode(255)
tim.penup()
tim.setheading(225)
tim.hideturtle()
tim.forward(300)
tim.setheading(0)

def rand_color():
    r = random.randint(0,255)
    g = random.randint(0,255)
    b = random.randint(0,255)
    random_color = (r,g,b)
    return random_color

for i in range(1,101):
    tim.dot(30, rand_color())
    tim.forward(50)

    if i>=10 and i%10 == 0:
        tim.left(90)
        tim.forward(50)
        tim.left(90)
        tim.forward(500)
        tim.setheading(0)


screen.exitonclick()
